{
  "name": "dudes-workspace",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=./apps/*",
    "build": "turbo build --filter=./apps/*",
    "format": "prettier --write \"**/*.{js,ts,json}\""
  },
  "devDependencies": {
    "@crashmax/prettier-config": "5.0.2",
    "turbo": "1.12.2"
  },
  "pnpm": {
    "patchedDependencies": {
      "v-tweakpane@0.2.0": "patches/v-tweakpane@0.2.0.patch"
    }
  }
}
